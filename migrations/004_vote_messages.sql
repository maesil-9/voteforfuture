-- ============================================================
-- 004: 투표 시 한 마디 메시지 (선택, 50자)
--
-- 메시지 원문은 선택값과 함께 AES-256-GCM 암호문 안에 봉인된다.
-- 이 컬럼은 "메시지를 남겼는지 여부"만 기록한다 — 검수 화면에서는
-- 존재 여부만 보이고, 내용은 결과 공개 후 익명으로만 열람된다.
-- ============================================================

alter table vote_submissions
  add column has_message boolean not null default false;
